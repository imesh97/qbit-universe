[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qbitUni"
version = "0.1.0"
description = "A quantum universe simulator with a C-engine"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy",
]